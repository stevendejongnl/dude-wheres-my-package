{
  "name": "dude-wheres-my-package",
  "private": true,
  "scripts": {
    "build": "esbuild src/dwmp/static/ts/index.ts --bundle --minify --outfile=src/dwmp/static/js/notifications.js --format=iife",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "pack:extension": "node scripts/pack-extension.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "crx3": "^1.1.3",
    "esbuild": "^0.28.0",
    "jsdom": "^29.0.2",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  }
}
